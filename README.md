ChatGPT said:
# 📊 LLM-Powered Exploratory Data Analysis (EDA)

A lightweight AI-assisted EDA tool built using **Gradio**, **Pandas**, **Matplotlib/Seaborn**, and **Ollama (Mistral)**.  
Upload any CSV file and get:

- Automated data cleaning  
- Summary statistics  
- Missing value report  
- Visualizations (histograms + correlation heatmap)  
- Natural-language insights generated by an LLM  

---

## 🚀 Features
- 🔍 **Automatic EDA** on any CSV file  
- 🧹 **Missing value handling**  
  - Numeric → median  
  - Categorical → mode  
- 📈 **Visualizations generated automatically**  
  - Distribution plots for all numeric columns  
  - Correlation heatmap  
- 🤖 **LLM Insights** powered by a local Ollama model (`mistral`)  
- 🌐 **Gradio Web UI** with EDA report + gallery of plots  

---

## 📦 Requirements
Install dependencies:

```bash
pip install gradio pandas matplotlib seaborn ollama

Ollama Requirements

Install Ollama: https://ollama.com

Pull the required model:

ollama pull mistral


Ensure Ollama daemon is running.

🛠️ How to Run
python app.py


You will see a Gradio URL like:

Running on http://127.0.0.1:7860


If share=True is enabled, an additional public URL is generated.

📁 Project Structure
project/
│── app.py
│── requirements.txt   (optional)
│── *.png              (generated plots)

🧠 How It Works

User uploads a CSV file.

Script loads & cleans the data (median/mode fill).

Summary statistics & missing-value reports are generated.

Visualizations are saved locally and returned as a gallery.

The summary text is sent to Mistral (Ollama) for AI insights.

Gradio displays both the report + visualizations.

🖼️ Generated Outputs

<column>_distribution.png (one for each numeric column)

correlation_heatmap.png (if numeric columns exist)
